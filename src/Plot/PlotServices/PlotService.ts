import { SavePlotData } from "../PlotInterfaces/SavePlotData";

export const generateShareURL = (plotData: SavePlotData) => {
    // Do some stuff here to initialise the url

    return plotData.id;
};
