import { SavePlotData } from '../../PlotInterfaces/SavePlotData'
import databaseConnection from '../../../databaseConnection'

export const uploadPlotData = (plotData: SavePlotData) => {
    // TODO: use connect to database and insert data into relevant tables
}